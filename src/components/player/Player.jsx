import React from 'react';

const Player = ({eachPlayer}) => {
    console.log(eachPlayer)
    return (
        <div>
            <img src="" alt="" />
        </div>
    );
};

export default Player;